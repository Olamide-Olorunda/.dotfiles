[opener]
pdf = [
  { run = "open -a Zathura \"$@\"", desc = "Zathura", block = false }
]
epub = [
  {run = "open -a Zathura \"$@\"", desc = "Zathura", block = false }
]

[open]
rules = [
  { mime = "application/pdf", use = "pdf" },
  { mime = "application/epub+zip", use = "epub" },
]
